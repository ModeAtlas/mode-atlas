name: Analyze 2.39.0 trainer controller parity
on:
  push:
    branches:
      - agent/mode-atlas-2.39.0-trainer-controller-consolidation
    paths:
      - .github/trigger-2390-analysis.txt
permissions:
  contents: read
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compare Reading and Writing controllers
        run: python3 .github/analyze_2390.py
